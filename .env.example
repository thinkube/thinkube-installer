# Thinkube Installer - Environment Variables Example
#
# Copy this file to one of the following locations:
#   1. ~/.thinkube-installer/.env  (recommended for user-specific config)
#   2. ~/.env                      (home directory)
#   3. Current directory/.env      (project root)
#
# The installer will automatically load these values and prepopulate
# the configuration screen fields. You can still override them in the UI.

# Cloudflare API Token for DNS management
# Get from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_TOKEN=your_cloudflare_api_token_here

# ZeroTier Network Configuration
# Get from: https://my.zerotier.com
ZEROTIER_API_TOKEN=your_zerotier_api_token_here
ZEROTIER_NETWORK_ID=your_zerotier_network_id_here

# GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# Requires: repo, read:org, read:packages
GITHUB_TOKEN=ghp_your_github_personal_access_token_here
GITHUB_ORG=your_github_organization_name

# Cluster Configuration
CLUSTER_NAME=thinkube
DOMAIN_NAME=my-homelab.com
